import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import BaseText from './index.vue'

<Meta
  title="Atoms/BaseText"
  component={BaseText}
  argTypes={{
    size: {
      options: ['small', 'medium', 'large'],
      control: { type: 'inline-radio' },
    },
    color: {
      options: ['default', 'light', 'alert'],
      control: { type: 'inline-radio' },
    },
  }}
/>

export const Template = (args) => ({
  components: { BaseText },
  setup() {
    return { args }
  },
  template: `<base-text v-bind="args">Nome is all-in-one workspace.</base-text>`,
})

# BaseText

slotで受け取ったテキストを表示する。

<Canvas>
  <Story
    name="Default"
    args={{ size: 'medium', color: 'default', bold: false }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />